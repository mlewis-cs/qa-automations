# MCP scaffold (Playwright QA)

This is a minimal scaffold for a Python-based MCP server that will:
- list available actions from page classes
- run actions against a Playwright-owned browser session
- log actions and CLI fallbacks

Nothing here is wired to Playwright yet. This is the skeleton we'll iterate on.